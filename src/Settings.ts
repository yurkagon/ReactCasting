class Settings {
  public static readonly windowWidth: number = window.innerWidth * 0.8;
  public static readonly windowHeight: number = window.innerHeight * 0.8;

  private constructor() {}
}

export default Settings;
